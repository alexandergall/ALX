Release Notes for 15.09.ALX.25.0a31f16

The app apps.tap.tap, which provides access to a Linux TAP device, has
been enhanced to export the MTU, MAC address, interface status and
interface type as statistics counters.

A TAP interface can be used as attachment circuit for a VPLA instance
just like a physical interface.
